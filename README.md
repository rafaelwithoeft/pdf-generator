# PDF generator

This PDF use random data of products with images.
Using a sample of 2000~4000 mock produts using `json-schema-faker`, you can change it on `mock.ts`.

After HTML is mounted, a PDF file will be saved on root folder using `puppeteer`.

To run: `yarn dev`
